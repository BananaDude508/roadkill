using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // This prints Test! to the debug log
        Debug.Log("Test!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
